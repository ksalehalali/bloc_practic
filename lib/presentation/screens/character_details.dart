import 'package:flutter/material.dart';

class CaracterDetailsScreen extends StatelessWidget {
  const CaracterDetailsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Details'),
    );
  }
}
